export interface Product {
    name: string,
    Image: string,
    contCode: string,
    price: number,
    id: string,
    location: string,
    state: string
    borrowedBy: string;
    requestCount: number
}